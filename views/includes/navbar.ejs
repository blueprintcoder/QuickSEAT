<nav class="navbar">
    <div class="nav-container">
        <!-- Logo -->
        <div class="nav-brand">
            <a href="/">🍽️ QuickSEAT</a>
        </div>

        <!-- Desktop Navigation Links -->
        <ul class="nav-links">
            <li><a href="/restaurants#all-restaurants-list">All Restaurants</a></li>

            <% if (locals.currentUser) { %>
                <li><a href="/my-bookings">My Bookings</a></li>
            <% } %>

            <% 
            if (locals.currentUser && !locals.isRestaurantManager) { 
            %>
                <li><a href="/restaurants/new" class="btn-primary-small">Add Restaurant</a></li>
            <% } %>
            
            <% if (locals.isRestaurantManager) { %>
                <li><a href="/restaurant-dashboard" class="btn-primary-small">Dashboard</a></li>
            <% } %>
        </ul>

        <!-- Desktop Auth Section -->
        <div class="nav-auth">
            <% if (locals.isRestaurantManager) { %>
                <div class="user-menu">
                    <span class="user-greeting">🍴 <%= locals.restaurantName %></span>
                    <a href="/logout" class="btn-logout-nav">Logout</a>
                </div>
            <% } else if (locals.currentUser) { %>
                <div class="user-menu">
                    <span class="user-greeting">👤 <%= locals.currentUser.fullName %></span>
                    <a href="/logout" class="btn-logout-nav">Logout</a>
                </div>
            <% } else { %>
                <a href="/login" class="btn-login-nav">Login</a>
                <a href="/signup" class="btn-signup-nav">Sign Up</a>
            <% } %>
        </div>

        <!-- Mobile Hamburger Button (NEW) -->
        <button class="mobile-menu-toggle" id="mobileMenuBtn">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>

    <!-- Mobile Menu (NEW) -->
    <div class="mobile-menu" id="mobileMenu">
        <a href="/restaurants#all-restaurants-list">All Restaurants</a>

        <% if (locals.currentUser) { %>
            <a href="/my-bookings">My Bookings</a>
        <% } %>

        <% if (locals.currentUser && !locals.isRestaurantManager) { %>
            <a href="/restaurants/new">Add Restaurant</a>
        <% } %>
        
        <% if (locals.isRestaurantManager) { %>
            <a href="/restaurant-dashboard">Dashboard</a>
        <% } %>

        <!-- Mobile Auth Section -->
        <div class="mobile-auth-section">
            <% if (locals.isRestaurantManager) { %>
                <div class="mobile-user-menu">
                    <span class="mobile-user-greeting">🍴 <%= locals.restaurantName %></span>
                    <a href="/logout" class="btn-logout-mobile">Logout</a>
                </div>
            <% } else if (locals.currentUser) { %>
                <div class="mobile-user-menu">
                    <span class="mobile-user-greeting">👤 <%= locals.currentUser.fullName %></span>
                    <a href="/logout" class="btn-logout-mobile">Logout</a>
                </div>
            <% } else { %>
                <a href="/login" class="btn-login-mobile">Login</a>
                <a href="/signup" class="btn-signup-mobile">Sign Up</a>
            <% } %>
        </div>
    </div>
</nav>

<style>
/* ==================== NAVBAR BASE ==================== */
.navbar {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(50px);
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 1000;
    padding: 0.8rem 0;
}

.nav-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.nav-brand a {
    color: #e74c3c;
    font-size: 1.8rem;
    font-weight: 700;
    text-decoration: none;
}

/* ==================== DESKTOP: NAV LINKS (show by default) ==================== */
.nav-links {
    display: flex;
    list-style: none;
    gap: 2rem;
    align-items: center;
}

.nav-links a {
    text-decoration: none;
    color: #333;
    font-weight: 500;
    transition: color 0.3s ease;
}

.nav-links a:hover {
    color: #e74c3c;
}

/* ==================== DESKTOP: AUTH SECTION (show by default) ==================== */
.nav-auth {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.user-menu {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.user-greeting {
    color: #333;
    font-weight: 600;
}

.btn-login-nav,
.btn-signup-nav,
.btn-logout-nav {
    padding: 0.6rem 1.5rem;
    border-radius: 25px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
}

.btn-login-nav {
    color: #667eea;
    border: 2px solid #667eea;
    background-color: white;
}

.btn-login-nav:hover {
    background-color: #667eea;
    color: white;
}

.btn-signup-nav {
    background-color: #e74c3c;
    color: white;
    border: 2px solid #e74c3c;
}

.btn-signup-nav:hover {
    background-color: #c0392b;
    border-color: #c0392b;
}

.btn-logout-nav {
    background-color: #95a5a6;
    color: white;
    border: 2px solid #95a5a6;
}

.btn-logout-nav:hover {
    background-color: #7f8c8d;
    border-color: #7f8c8d;
}

/* ==================== MOBILE: HAMBURGER BUTTON ==================== */
.mobile-menu-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    margin: 0;
}

.mobile-menu-toggle span {
    width: 25px;
    height: 3px;
    background-color: #333;
    border-radius: 2px;
    transition: all 0.3s ease;
}

.mobile-menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(10px, 10px);
}

.mobile-menu-toggle.active span:nth-child(2) {
    opacity: 0;
}

.mobile-menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(8px, -8px);
}

/* ==================== MOBILE: MENU DROPDOWN ==================== */
.mobile-menu {
    display: none;
    position: absolute;
    top: 60px;
    left: 0;
    right: 0;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    flex-direction: column;
    padding: 1rem 0;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    z-index: 999;
}

.mobile-menu.active {
    display: flex;
}

.mobile-menu a {
    padding: 1rem 1.5rem;
    color: #333;
    text-decoration: none;
    border-bottom: 1px solid #f0f0f0;
    font-weight: 500;
    transition: background-color 0.3s ease;
}

.mobile-menu a:hover {
    background-color: #f5f5f5;
    color: #e74c3c;
}

/* Mobile Auth Section */
.mobile-auth-section {
    padding: 1rem 0;
    border-top: 1px solid #f0f0f0;
    display: flex;
    flex-direction: column;
}

.mobile-user-menu {
    display: flex;
    flex-direction: column;
    padding: 1rem 1.5rem;
    gap: 0.8rem;
}

.mobile-user-greeting {
    color: #333;
    font-weight: 600;
}

.btn-login-mobile,
.btn-signup-mobile,
.btn-logout-mobile {
    padding: 0.8rem 1.5rem;
    margin: 0.5rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    text-align: center;
    cursor: pointer;
    border: none;
    transition: all 0.3s ease;
}

.btn-login-mobile {
    background-color: white;
    color: #667eea;
    border: 2px solid #667eea;
}

.btn-login-mobile:hover {
    background-color: #667eea;
    color: white;
}

.btn-signup-mobile {
    background-color: #e74c3c;
    color: white;
    border: 2px solid #e74c3c;
}

.btn-signup-mobile:hover {
    background-color: #c0392b;
    border-color: #c0392b;
}

.btn-logout-mobile {
    background-color: #95a5a6;
    color: white;
    border: 2px solid #95a5a6;
}

.btn-logout-mobile:hover {
    background-color: #7f8c8d;
    border-color: #7f8c8d;
}

/* ==================== RESPONSIVE: TABLET & MOBILE ==================== */
@media (max-width: 768px) {
    /* Hide desktop elements */
    .nav-links {
        display: none !important;
    }

    .nav-auth {
        display: none !important;
    }

    /* Show hamburger button */
    .mobile-menu-toggle {
        display: flex !important;
    }

    /* Adjust navbar padding */
    .nav-container {
        padding: 0 15px;
    }

    .nav-brand a {
        font-size: 1.4rem;
    }

    /* Push hero section down */
    .hero {
        margin-top: 60px;
    }
}

@media (max-width: 480px) {
    .nav-brand a {
        font-size: 1.2rem;
    }

    .mobile-menu-toggle {
        padding: 0.3rem;
    }

    .mobile-menu-toggle span {
        width: 20px;
        height: 2.5px;
    }

    .mobile-menu a {
        padding: 0.8rem 1rem;
        font-size: 0.95rem;
    }

    .mobile-user-menu {
        padding: 0.8rem 1rem;
    }

    .btn-login-mobile,
    .btn-signup-mobile,
    .btn-logout-mobile {
        padding: 0.7rem 1rem;
        margin: 0.5rem 1rem;
        font-size: 0.95rem;
    }
}

/* ==================== DESKTOP: Keep showing nav elements ==================== */
@media (min-width: 769px) {
    .mobile-menu-toggle {
        display: none !important;
    }

    .mobile-menu {
        display: none !important;
    }

    .nav-links {
        display: flex !important;
    }

    .nav-auth {
        display: flex !important;
    }

    .hero {
        margin-top: 0;
    }
}
</style>